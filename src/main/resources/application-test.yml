spring:
  jpa:
    properties:
      hibernate:
        show_sql: false

falapet:
  http:
    cursor:
      signing-key: test-only-cursor-signing-key-32-bytes

logging:
  structured:
    format:
      console:
